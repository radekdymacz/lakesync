{
	"name": "@lakesync/connector-jira",
	"version": "0.1.0",
	"files": [
		"src/**/*.ts",
		"!src/**/__tests__/**"
	],
	"type": "module",
	"description": "Jira Cloud source connector for LakeSync — polls issues, comments, and projects into the sync gateway",
	"keywords": [
		"sync",
		"lakesync",
		"jira",
		"connector",
		"source"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/radekdymacz/lakesync.git",
		"directory": "packages/connector-jira"
	},
	"license": "Apache-2.0",
	"author": "Radek Dymacz",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc --noEmit",
		"test": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"peerDependencies": {
		"@lakesync/core": "workspace:*",
		"@lakesync/gateway": "workspace:*"
	},
	"devDependencies": {
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
